generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model PrismaSmokeTest {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
}
